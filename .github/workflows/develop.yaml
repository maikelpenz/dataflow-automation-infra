name: 'Develop - GitHub Actions'

on:
  push:
    branches:
        - master

env:
    tf_version: 'latest'
    tf_working_dir: './tf-automation'
    env: 'dev'
    aws_region: 'ap-southeast-2'
    artifacts_bucket: 'mamip-artifacts'